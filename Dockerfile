# Figure out base image...
# FROM python:3

# install git
RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y git

# install golang
	# install CLI library
	# figure out some server
	# yml parser

# install python 3.7
	# install pandas
	# install matplotlib

# install node 10
	# install d3

# I will want to create a CLI project
# grab the code from that from local repo

# maybe set up a test directory -> TDD
